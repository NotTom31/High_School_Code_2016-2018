/*problem #9 (The instructions make absolutely no sense? 
is it asking for a functioning program or just a bunch of random nonsense?)*/
#include <iostream>

using std::cin;
using std::cout;

int main(){
    int x, y, z, a, b, c, d; 
    cout << "Enter 3 integers: ";
    cin >> x >> y >> z;
    z = x + y;
    cout << "a program calculates the average marks in three subjects\n";
    cin >> a >> b >> c >> d;
}